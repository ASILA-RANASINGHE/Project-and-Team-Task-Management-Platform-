{
  "name": "project-and-team-task-management-platform",
  "version": "1.0.0",
  "description": "A full-stack project and team task management platform with role-based access control.",
  "main": "src/app.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "build": "npx prisma generate",
    "lint": "eslint .",
    "test": "jest --forceExit --detectOpenHandles",
    "test:ci": "jest --forceExit --detectOpenHandles --ci --coverage",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "keywords": [
    "project-management",
    "task-management",
    "express",
    "prisma"
  ],
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcrypt": "^5.1.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "prisma": "^6.19.3",
    "supertest": "^7.0.0"
  }
}
